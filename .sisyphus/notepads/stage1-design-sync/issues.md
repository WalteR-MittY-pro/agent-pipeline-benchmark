- Review finding (Stage 1 alignment): github_client.py:list_prs() still queries closed PRs with sort=updated and slices before filtering merged PRs, so it does not satisfy the documented merged_at-descending contract and can miss older merged PRs when recent closed-but-unmerged PRs occupy the first page.
- Review finding (Stage 1 alignment): nodes/fetch_prs.py:fetch_prs() only checks target_items at repo boundaries; once the threshold is reached mid-repo it keeps appending PRs from that repo, so Stage 1 can overshoot the documented global candidate cap.
- Fix applied: `list_prs()` now filters all merged PRs before truncating to `max_n`, then sorts the final list by `merged_at` descending; `fetch_prs()` now checks `target_items` inside the PR loop so it stops without overshooting.
- Re-review finding (Stage 1 alignment): github_client.py:list_prs() logic now filters/sorts/truncates correctly on cache miss, but it still uses the same cache key format (prs:{repo}:{max_n}); existing cached entries produced under the old semantics will still be returned for up to 6 hours, so the fix is not reliably applied in persistent benchmark_runs.db runs.
